server:
  port: ${SERVER_PORT:8083}

spring:
  application:
    name: trade-fulfillment-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  info:
    env:
      enabled: true
    git:
      mode: simple

info:
  app:
    name: ${spring.application.name}
