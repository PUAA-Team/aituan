spring:
  datasource:
    url: jdbc:h2:mem:aituan_trade_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
    clean-disabled: false

aituan:
  internal:
    service-token: test-internal-token
  trade:
    delivery-scheduler:
      enabled: false
