server:
  port: ${SERVER_PORT:8084}

spring:
  application:
    name: engagement-platform-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  info:
    env:
      enabled: true
    git:
      mode: simple

info:
  app:
    name: ${spring.application.name}
