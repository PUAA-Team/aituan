apiVersion: apps/v1
kind: Deployment
metadata:
  name: engagement-platform-service
  namespace: aituan
  labels:
    app: engagement-platform-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: engagement-platform-service
  template:
    metadata:
      labels:
        app: engagement-platform-service
    spec:
      containers:
        - name: engagement-platform-service
          image: ghcr.io/puaa-team/aituan/engagement-platform-service:sha-placeholder
          ports:
            - containerPort: 8084
          env:
            - name: SERVER_PORT
              value: "8084"
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: AITUAN_PLATFORM_DB_URL
              valueFrom:
                secretKeyRef:
                  name: engagement-platform-db-secret
                  key: url
            - name: AITUAN_PLATFORM_DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: engagement-platform-db-secret
                  key: username
            - name: AITUAN_PLATFORM_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: engagement-platform-db-secret
                  key: password
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 45
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 768Mi
---
apiVersion: v1
kind: Service
metadata:
  name: engagement-platform-service
  namespace: aituan
spec:
  selector:
    app: engagement-platform-service
  ports:
    - name: http
      port: 8084
      targetPort: 8084
