# 成员 E 进度追踪

> 维护规则：完成一项打 `[x]`，括号里写 commit hash 或日期。阻塞项写到末尾「当前阻塞」。

## 第一轮：数据库与后端接口骨架

### 数据库
- [x] V009__interaction_governance_init.sql 草稿（2026-05-22）
- [x] V009 在 demo profile 干净迁移（H2 MySQL 模式 9 个 migration 全部通过）
- [x] R__seed 增加评价/客服/投诉演示数据并保持幂等（2026-05-29：demo/test profile 均随 Flyway repeatable seed 干净迁移）

### 后端
- [x] `interaction` 模块扩展 me / helpful / report / merchant reply / admin audit
- [x] `support` 模块新建（Controller / Service / Repository / Dtos）
- [x] `complaint` 模块新建
- [x] `statistics` 模块新建（商家驾驶舱 + 后台治理驾驶舱）
- [x] `admin` 模块 dashboard 字段扩展（合并到 statistics 独立端点 `/api/admin/governance/dashboard`，admin 模块零改动）
- [x] 单元测试：评价回复幂等（2026-05-29：`InteractionServiceTest` 通过）
- [x] 单元测试：投诉状态机非法跃迁（2026-05-29：`ComplaintServiceTest` 通过）
- [x] 单元测试：客服会话归属校验（2026-05-29：`SupportServiceTest` 通过）
- [x] 用 curl 验证全部新接口（用户端 me/helpful/report、商家端 review/reply/sessions/templates、后台 audit/complaints accept/resolve/close、双驾驶舱）

## 第二轮：用户端补齐

- [x] `review_publish_page.dart` 接入后端，删除硬编码（2026-05-23：已接入 `/api/app/interaction/orders/{orderId}/review`，含 9 张图片占位上传）
- [x] 新建「我的评价」页 `my_reviews_page.dart`（2026-05-23：列表 + 状态筛选）
- [x] 新建评价详情页 `review_detail_page.dart`（2026-05-23：含图片/标签/商家回复/有用/举报）
- [x] 评价点"有用"与举报按钮（2026-05-23：详情页底部）
- [x] 新建 `features/support/` 目录与会话列表/详情页（2026-05-23：含从商家页发起新会话流程，已存在 open 会话直接复用）
- [x] 新建 `features/complaint/` 目录与投诉提交页（2026-05-23：从订单详情/我的页面进入，预填 orderId）
- [x] 消息中心列表增加"评价/咨询/投诉"分组（2026-05-24：按 type 分组 + ChoiceChip 切换，复用现有 `/api/app/message/station` 返回的 type 字段）
- [x] 路由与路由常量同步更新（2026-05-23：含 `SupportLaunchArgs`、`ComplaintSubmitArgs`、`ReviewArgs`；profile/订单/商家页全部接通）
- [x] `flutter analyze` 通过（2026-05-29：No issues found）
- [x] `flutter test` 通过（2026-05-29：widget_test 全部通过）

## 第三轮：商家端与后台端

### 商家端
- [x] `ReviewPage.vue` 评价列表 + 回复（2026-05-24：状态/回复筛选 + 详情卡 + 回复输入；接 `/api/merchant/ops/reviews*`）
- [x] `SessionsPage.vue` 客服会话 + 快捷回复模板（2026-05-24：会话列表 + 消息气泡 + 模板下拉 + 关闭会话）
- [x] `DashboardPage.vue` 商家驾驶舱（2026-05-23 已存在，接 `/api/merchant/ops/dashboard`，含近 7 天柱状图）
- [x] 左侧菜单/路由同步（2026-05-24：ConsoleFrame 加 dashboard/reviews/sessions；App.vue 默认进 dashboard 页）

### 后台端
- [x] `ReviewsPage.vue` 评价审核（2026-05-24：通过/屏蔽/恢复 + 备注 + 被举报筛选）
- [x] `ComplaintsPage.vue` 投诉工单（2026-05-24：受理/处理/关闭 + 状态机校验 + 分类筛选）
- [x] `AuditLogsPage.vue` 审计日志独立页（2026-05-24：从 SettingsPage 抽出 + 按 actorType / actionType 筛选）
- [x] 现有 `DashboardPage.vue` 扩展三块指标（2026-05-24：待审核评价 / 被举报 / 待处理投诉 / 进行中咨询 + 评分分布条形图）
- [x] 左侧菜单/路由同步（2026-05-24：AdminFrame 加 reviews/complaints/audit 三项；SettingsPage 改名为「平台设置」并移除审计 panel）

### 构建
- [x] `npm --prefix apps/merchant_web run build` 通过（2026-05-29：Vite 生产构建成功）
- [x] `npm --prefix apps/admin_web run build` 通过（2026-05-29：Vite 生产构建成功；管理员默认密码已改为 seed 一致的 `123456`）

## 第四轮：联调 / 演示 / 出包

- [x] R__seed 最终版（评价 / 投诉 / 客服 / 审计跨日数据）（2026-05-29：评价 4 条、客服会话 4 条、投诉 3 条、审计 14 条）
- [x] 端到端演示：用户端下单→完成→评价→收回复→看回复（2026-05-29：最终 API 验收通过，新建外卖订单 #9020，商家推进到 `completed/used`，用户发布评价 #9020，米村拌饭商家回复后用户评价详情可见回复）
- [x] 端到端演示：用户端商家咨询→商家回复→关闭会话（2026-05-29：模拟器用户端发送 `hello`，米村拌饭商家账号回复并关闭，会话列表显示“已关闭”和最新商家回复）
- [x] 端到端演示：用户端投诉→后台处理→工单关闭（2026-05-29：模拟器提交 `demo_complaint`，后台受理→处理完成→关闭，用户侧投诉列表返回 `closed`）
- [x] 端到端演示：商家端驾驶舱与评价/咨询处理（2026-05-29：Computer Use 验证商家驾驶舱、评价管理、客服会话页面；API 验证米村拌饭咨询回复与关闭）
- [x] 端到端演示：后台端驾驶舱/审核/投诉/审计（2026-05-29：Computer Use 验证平台总览、评价审核、投诉工单、审计日志；投诉 #4 审计含 submit/accept/resolve/close）
- [x] Windows 本机 APK：`D:/aituan_release/apk/aituan-user-debug.apk`（2026-05-30：`scripts/build/build_android_apk.ps1` 成功）
- [x] 写 `docs/stage6-memberE/交付说明.md`（2026-05-29）
- [x] 把所有新文档加进 `docs/ReadMe.md`（2026-05-29：新增交付说明索引）

---

## 当前阻塞

> 写下当下解不开的问题、需要等队友/老师/工具的点。

- 2026-05-29：原 Web 构建阻塞已解除，`npm ci` 后商家端/后台端 build 均通过。
- 2026-05-29：完整评价链路已通过本机最终 API 验收；订单 #9020 从下单、支付、商家履约完成到评价 #9020 发布、商家回复、用户详情查看回复均通过。
- Windows 本机 APK 已通过脚本输出 `D:/aituan_release/apk/aituan-user-debug.apk`，后续集成分支验证继续沿用该路径。

## 已知 TODO 待沉淀

> 临时想到、但本阶段不做的功能，及时挪去 `docs/后续功能待办.md`。

- 评价图片真实上传到对象存储（暂时本地文件夹）
- 投诉证据图/视频上传
- AI 评价违规识别
- 评价红包 / 评价 KOL 体系
