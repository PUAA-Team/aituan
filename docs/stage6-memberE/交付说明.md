# 成员 E 交付说明

> 更新时间：2026-05-30 00:00 CST  
> 本地验证环境：Windows 11 / PowerShell / JDK 17 / Maven 3.9.14 / Flutter 3.41.6

## 交付范围

- 后端：评价互动、客服会话、投诉工单、商家驾驶舱、后台治理驾驶舱、审计日志相关接口。
- 用户端：评价发布、我的评价、评价详情、客服会话、投诉提交入口与页面。
- 商家端：评价管理、客服会话、商家驾驶舱。
- 后台端：评价审核、投诉工单、治理驾驶舱、独立审计日志页。
- 数据：`R__seed_demo_data.sql` 已补评价、客服、投诉、审计演示数据，并保持幂等。

## 本次验证

- `mvn -f services/backend/pom.xml -Dmaven.repo.local=D:/aituan_cache/m2 clean package`：构建通过，jar 产物为 `services/backend/target/aituan-backend-0.0.1-SNAPSHOT.jar`。
- `npm --prefix apps/merchant_web --cache D:/aituan_cache/npm run build`：构建通过。
- `npm --prefix apps/admin_web --cache D:/aituan_cache/npm run build`：构建通过。
- `scripts/build/build_android_apk.ps1`：已执行 `flutter analyze`、`flutter test` 和 `flutter build apk --debug`，APK 产物为 `D:/aituan_release/apk/aituan-user-debug.apk`。
- Computer Use 页面烟测：
  - 商家端：登录、商家驾驶舱、评价管理、客服会话页面可渲染；驾驶舱显示今日订单、今日交易额、待处理评价、进行中咨询等指标。
  - 后台端：登录、平台总览、评价审核、投诉工单、审计日志页面可渲染；Chrome 密码安全提示关闭后登录正常。
- Android 模拟器逐屏验证（`emulator-5554`）：
  - 用户端登录、我的评价、评价详情、客服咨询、投诉提交页面可用。
  - 客服会话 `米村拌饭 / SS202605170002`：用户端发送 `hello`，商家端回复“您好，已收到咨询，配送时长会尽量加快。”并关闭；用户端会话列表显示该会话 `已关闭`，最新消息为商家回复。
  - 投诉工单 `demo_complaint / CP1780047022567-1`：用户端提交后，后台受理、处理完成、关闭；后台投诉列表显示 `已关闭`，审计日志包含 `complaint_submit / complaint_accept / complaint_resolve / complaint_close`。
- 本地 demo 后端接口烟测：
  - 用户评价列表：返回 4 条。
  - 用户客服会话：返回 4 条。
  - 用户投诉列表：本轮提交后返回 4 条，其中 `demo_complaint` 状态为 `closed`。
  - 商家驾驶舱：返回 success，openSessions = 1。
  - 米村拌饭商家咨询：会话 #2 回复后关闭，用户端详情可见商家回复和 `closed` 状态。
  - 后台治理驾驶舱：返回 success，pendingReviews = 1，pendingComplaints 随本轮投诉处理前后动态变化，openSessions 随咨询关闭动态变化。
  - 后台评价审核列表：返回 4 条。
  - 后台投诉工单列表：本轮提交后返回 4 条。
  - 后台审计日志：本轮新增 `demo_complaint` 的提交、受理、处理、关闭记录。
  - 完整评价链路最终验收：新建外卖订单 #9020，支付后由米村拌饭商家账号推进 `accept → prepare → ready → delivering → delivered → complete`，用户发布评价 #9020，商家回复“感谢您的最终验收反馈，成员E评价回复链路已通过！”，用户评价详情返回 `replied=true` 且回复内容可见。

## 演示账号

| 端 | 账号 | 密码 |
| --- | --- | --- |
| 用户端 | `demo_user` | `123456` |
| 商家端 | `demo_merchant` | `123456` |
| 后台端 | `demo_admin` | `123456` |

后台端和用户端登录页默认密码已同步为 `123456`，避免 seed 账号演示登录失败。

## 仍需人工/设备演示

- 如课程验收需要录屏材料，可基于本轮已通过的订单 #9020 / 评价 #9020 路径补录用户端逐屏视频。
- Windows 本地构建路径仍按 `D:/aituan_release/apk/aituan-user-debug.apk`，后续部署前继续使用同一脚本验证。
