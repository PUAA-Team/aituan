# 软件工程基础实践第二阶段完成情况与剩余工作

> 更新日期：2026-09-02  
> 验收基线：本次合入 `main` 的微服务集成版  
> 验收依据：`docs/stage-new-1/软工小学期.md`、`docs/stage-new-3/微服务并行拆分分工与统一标准.md`、当前仓库代码、GitHub Actions 和生产 k3s 实测结果

## 1. 结论

第二阶段的“业务微服务拆分”和“微服务 CI/CD”主体已完成。当前系统是 Gateway + A/B/C/D 四个业务微服务，具备独立构建、独立测试、独立镜像和独立 Kubernetes Workload；四个服务使用四个独立 schema 和四个不同数据库账号，全部 12 项跨库访问都由真实 MySQL 权限拒绝。

自动交付主链路是：

```text
main push
  -> aituan-microservices-ci（全量测试与 7 镜像可构建验证）
  -> aituan-microservices-deploy（校验同一 SHA）
  -> 复跑质量门禁
  -> 发布 7 个不可变 SHA 镜像
  -> k3s 滚动部署
  -> 5 服务版本/健康、7 镜像、三端、三角色登录、APK 复验
```

因此，“合拢后能否启动、能否对接、是否有完整微服务 CI/CD”已经具备通过条件。多服务 HPA 和依赖故障隔离不仅已实现，而且已经在生产 k3s 上完成真实扩缩容、停服、隔离和恢复演练并归档证据。单体与微服务也已在同一生产节点、同一 MySQL、同一份业务数据和固定单副本条件下完成各 3 轮性能对比，90000 个正式请求错误率为 0，课程 Part 2 的工程与实验项已闭环。

## 2. 课程要求对照

| 课程要求 | 状态 | 仓库或运行证据 | 剩余动作 |
| --- | --- | --- | --- |
| 至少拆分 3 个业务微服务 | 完成 | A 账号资产、B 商家商品、C 交易履约、D 互动平台，Gateway 不计入业务服务数 | 无 |
| 服务按业务划分 | 完成 | 227 个公开 HTTP 操作唯一归属 A 47 / B 65 / C 56 / D 59，Gateway 路由与 Controller inventory 有自动化契约 | 无 |
| 每个微服务可独立构建、测试、启动和部署 | 完成 | 5 个 Java Maven module（含 Gateway）、5 个 Dockerfile、5 组 Service/Deployment、CI 服务矩阵 | 无 |
| 数据所有权清晰，禁止跨服务直接读写 | 完成 | `aituan_identity` / `aituan_merchant` / `aituan_trade` / `aituan_platform`；四账号；12/12 跨 schema 拒绝；跨 schema FK=0 | 无 |
| 跨服务通过 API 对接 | 完成 | 真实 HTTP client、内部 Token/调用方白名单、requestId、幂等键、快照和补偿；无旧单体 `/api/**` fallback | 无 |
| 微服务 CI/CD | 完成 | 完整 CI 通过后才触发同 SHA CD；发布 Gateway+A/B/C/D+MySQL+Web 共 7 镜像；自动部署 k3s | 保留每次 Actions 原始报告和部署 artifact |
| 自动扩缩容 | 完成 | Gateway 与 A/B/C/D 共 5 个 `autoscaling/v2` HPA，均为 1–4 Pod、CPU 50%；生产实压 20,726 请求，五个服务均由 1 扩到 4，再全部缩回 1，错误率 0.936%、容器重启 0 | 保留 `experiments/hpa-b8660d3-pass1/` 原始证据 |
| 故障处理 | 完成 | C 持久化购物车商品快照；生产停 B 后读降级、写快速失败且无部分数据、移除可用，A/C/D 不联动退出；B 33 秒内恢复且 C 无需重启 | 保留 `experiments/catalog-fault-b8660d3-pass4/` 原始证据 |
| 改造前后性能测试 | 完成 | 同一 `aituan-new` 节点、MySQL、业务响应、并发 50、15000 请求/轮；单体与微服务各 3 轮，90000 请求错误率 0；原始 JSONL、资源采样和报告已归档 | 答辩时说明微服务吞吐 +45.707%、内存 +163.007% 及适用范围 |

## 3. 已完成的工程内容

### 3.1 代码、接口与数据

- 已将 A/B/C/D 四个分工分支合拢到同一可交付基线，共享面由集成版统一，不依赖分支名称。
- Gateway 是唯一公开 API 入口，`/internal/**` 不对外暴露，外部伪造的内部调用头会被清理。
- C 的下单、支付、商家接单、配送、完成与 D 的评价、A 的成长值/消息、B 的库存扣减已串成真实跨服务链路。
- 四库 Flyway 迁移和默认 seed 归属各自服务；正常启动使用课程默认演示数据，不包含压测或验收临时数据。
- 仓库仍只使用 README 原有的两份用户配置：`.config` 对应 `.config.example`，`deploy/.env` 对应 `deploy/.env.example`；真实密码不入库。

### 3.2 测试与质量门禁

`aituan-microservices-ci` 包含：

- 旧单体静态回归、Java 测试/JaCoCo 门禁和 MySQL 8 迁移 smoke；
- Gateway、A/B/C/D 五个 Java 模块独立 `clean verify`；
- Merchant/Admin 的 Vitest 覆盖率测试与生产构建；
- Flutter `analyze`、测试/覆盖率和 Web 生产构建；
- 离线消费者契约、真实 Provider 契约和跨服务 acceptance smoke；
- MySQL 8.4 四 schema 迁移、四账号所有权、12 项跨库拒绝和跨 schema FK 检查；
- 三端生产产物通过 Gateway 执行 UC01–UC13，实际为 15 条 Playwright 场景；
- Compose/Kubernetes 合约和 7 个生产镜像可构建验证；
- 无论成功或失败都生成可下载的原始流水线报告。

HPA、故障隔离和发布时序实现基线的完整 CI 证据为 GitHub Actions run `33626566312`，对应提交 `b8660d3e1a0db9dca0fd4d9d9793fed53e3fb5d4`，21 个 job 全部成功；该 run 的精确 SHA 随后触发 CD run `33627149918` 并成功部署。最终证据提交仍必须由新的同 SHA CI 成功记录触发下游 CD，不使用旧 run 绕过门禁。

### 3.3 CD 是否包含全部测试和微服务

包含，但要按两段式流程理解：

1. 第一段 `aituan-microservices-ci` 运行上一节的完整测试，特别是真实 MySQL 四库、跨服务链路和 UC01–UC13。
2. 第二段 `aituan-microservices-deploy` 仅接受第一段成功的精确 `head_sha`，再次运行静态检查、全部 Java `clean verify`、两个 Vue 端覆盖率测试、Flutter analyze/test 和三端生产构建。
3. 质量门禁通过后，CD 发布 `api-gateway`、`identity-asset-service`、`merchant-catalog-service`、`trade-fulfillment-service`、`engagement-platform-service`、`mysql`、`web` 共 7 个镜像。
4. k3s 部署后，CD 等待 MySQL StatefulSet 与 6 个 Deployment 滚动完成，检查 5 个 Java 健康/版本端点、7 个实际镜像标签、用户/商家/管理三端、三角色默认账号登录和 APK 下载。

真实 MySQL + 15 条 E2E 不在 deployment job 里重复跑，而是作为同 SHA 的必经上游门禁。如果它们失败，CD 不会发布或部署。这仍是完整 CD，同时避免在部署 job 中再启动一套重复集成环境。

### 3.4 生产部署

- 生产服务器为 `aituan-new`（4C16G），运行单节点 k3s，namespace 为 `aituan`。
- GitHub Repository Variables 保持 `AUTO_DEPLOY_PRODUCTION=true`、`DEPLOY_TARGET=k8s`、`K8S_NAMESPACE=aituan`。
- Production Environment 已使用新服务器的 SSH、known_hosts、kubeconfig、应用配置和四库密码；配置和密码不写入仓库。
- `aituan.2b.gs` 已指向新服务器，80/443 可达，Let's Encrypt 证书和续期后同步 k3s Secret 的 hook 已配置。
- 手工基线与自动 CD 已验证公网健康、三端页面、三角色登录、APK 和四库隔离；生产 HPA 与 B 故障恢复实验均在 CD 部署的不可变 SHA 镜像上通过。

## 4. 默认数据、配置与 APK

### 4.1 默认数据

生产部署使用各服务仓库内的 repeatable seed，已恢复为课程默认演示数据，不带压测产生的临时订单。默认账号如下，密码均为 `123456`：

| 端 | 账号 |
| --- | --- |
| 用户端 | `demo_user`，手机 `18800001111`，邮箱 `user@example.com` |
| 商家端 | `demo_merchant` |
| 管理端 | `demo_admin` |

### 4.2 配置文件

| 用途 | 真实文件 | 仓库模板 | 是否入库 |
| --- | --- | --- | --- |
| 应用业务配置 | `.config` | `.config.example` | 真实文件否，模板是 |
| 部署环境配置 | `deploy/.env` | `deploy/.env.example` | 真实文件否，模板是 |

k3s 从 GitHub Production Secrets 中生成 `aituan-app-config`、`aituan-service-secret`、MySQL root Secret 和四个服务数据库 Secret。本次没有增加第三份需要人工维护的配置模板。

### 4.3 APK

本次主要变更为微服务、流水线和部署配置，当 Flutter 源码和对外 API 基地址未变化时，服务端部署不要求重新打包 APK。`aituan-android-apk` 工作流会在需要发布新用户端版本时运行 analyze/test/build；微服务 CD 的部署后检查会验证服务器现有 APK 下载链接可用。

## 5. 课程验收收尾

### 5.1 改造前后性能对比

该项已完成。旧的 Apple M5 + H2 单体压测不再作为改造前后结论依据；本次在 `aituan-new` 重新部署隔离单体基准环境，与微服务共用同一 MySQL 进程，并将微服务默认商家商品 seed 复制到单体隔离 schema。三个业务响应删除动态请求元数据后哈希完全相同。

三轮均值显示微服务吞吐为 463.943 req/s，单体为 318.409 req/s；微服务平均/P95 分别降低 32.496%/26.487%，90000 个正式请求全部成功。微服务平均内存增加 163.007%，因此报告同时给出单体 CPU cgroup 饱和、B 服务热点和多 JVM 内存代价，不作“微服务天然更快”的泛化。完整报告见 `docs/stage-new-4/单体与微服务同条件三轮性能对比报告.md`。

### 5.2 过程与答辩材料

Part 0 的每日站会、GitHub Projects/CodeArts 看板、中期检查和最终答辩属于团队过程证据，无法由代码流水线代替。仓库已经留存 8 月 26、27、28、29、31 日和 9 月 1 日的站会正文或 PDF，但未发现 8 月 30 日、9 月 2 日记录，也无法仅凭仓库证明每次均在中午 12 点前将 200 字简报上传云盘。应按课程实际考勤日核对并补齐缺失日期，同时保存云盘链接或提交时间截图。

仓库已有“用于中期检查”的设计稿，但未发现中期检查本身的正式记录。如果检查已经完成，应补一份简短记录，至少包含检查日期、参与人、检查结论、老师反馈和返工闭环；如果尚未检查，则需实际完成后再归档。除每日站会完整留痕、中期检查记录、看板证据和最终答辩材料外，课程 Part 1、Part 2 的代码、测试、部署和实验交付项均已完成。

## 6. 最终验收清单

工程交付完成标志：

- [x] `main` 包含 Gateway 与 A/B/C/D 四个业务微服务。
- [x] 每个服务可独立构建、测试和容器化。
- [x] 四个 schema 与四个独立账号，无跨库读写或跨库 FK。
- [x] 真实跨服务业务链路通过 Gateway 验证。
- [x] 完整 CI 包含旧版门禁、四库、契约、UC01–UC13、三端和 7 镜像。
- [x] CD 不能绕过同 SHA 完整 CI，且保持自动生产部署。
- [x] k3s 清单、Secret、TLS、GHCR 拉取和公网入口已配置。
- [x] 正常启动使用默认 seed，仓库只保留两份原有配置模板。
- [x] Gateway 与 A/B/C/D 都配置 HPA，CD 验证 CPU 指标，实压脚本含扩容和缩容自动断言。
- [x] B 故障时 C 购物车使用持久化快照降级，写入保护、独立健康和恢复有自动化测试/实验脚本。
- [x] 保存 HPA 加压扩容与降压缩容的完整实验证据。
- [x] 保存依赖故障注入、隔离、恢复的完整实验证据。
- [x] 保存单体与微服务同条件各 3 轮性能原始结果，并完成吞吐、延迟、错误率、CPU、内存和屏障点分析。
- [ ] 按实际考勤日核对并补齐每日站会，保存中午 12 点前上传云盘的时间凭证。
- [ ] 团队人工确认 GitHub Projects/CodeArts 每日看板截图或链接完整。
- [ ] 归档中期检查日期、参与人、结论、老师反馈和返工闭环记录。
- [ ] 准备最终答辩 PPT、演示脚本、分工说明和备用演示材料。
