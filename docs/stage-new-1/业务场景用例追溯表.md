# 业务场景追溯表

## 1. 编写说明

本文按照最终版文档中确定的 13 个核心业务场景（UC01-UC13）进行追溯整理。本文每个用例单独成节，分别追溯到需求/用户故事、用例说明、三层模型、代码模块、测试编号与测试结果。

追溯链路采用：

```text
需求 / 用户故事 -> 业务用例 -> 系统级模型 -> 概要/组件级模型 -> 详细/对象级模型 -> 代码模块 -> 测试编号 -> 测试结果
```

## 2. UC01 用户注册、登录并进入受保护业务

| 追溯项 | 内容 |
| --- | --- |
| 需求编号 | `REQ-F-001` |
| 用户故事 | `US-01`：作为用户，我希望注册、登录并进入受保护业务，以便安全使用本人有权访问的功能。 |
| 用例编号 | `UC-01` / `UC01` |
| 主要参与者 | 用户、验证码服务。 |
| 前置条件 | 客户端可访问认证入口；注册账号尚未被占用；用户端可访问后端认证接口；账号未被禁用。 |
| 主成功流程 | 用户提交注册信息并完成验证码校验；系统创建有效账户；用户提交登录凭据；系统校验账户状态并签发登录态；用户访问订单、个人中心等受保护业务；系统校验登录态后允许访问。 |
| 备选/异常流程 | 账号重复、验证码失效、密码错误或账号禁用时拒绝注册或登录；登录态失效或用户无权访问目标资源时拒绝访问并要求重新认证；Token 过期或角色不匹配时返回 401/403。 |
| 可验证结果 | 成功时用户获得有效登录态并进入受保护业务；失败时不泄露受保护数据并记录必要安全日志；未登录访问订单、会员、投诉等受保护页面或接口会被拦截。 |
| 三层模型：系统级 | `SYS-SEQ-01` 用户注册、登录与受保护业务访问系统级顺序图；对应需求说明书 3.6.1。 |
| 三层模型：概要/组件级 | 账号与用户资产子系统；认证接口组 `/api/open/auth`；核心组件包括 `AuthController`、`AuthService`、`AuthRepository`、`JwtTokenService`、`AccountController`；受保护业务统一通过 JWT 与角色边界校验。 |
| 三层模型：详细/对象级 | `OBJ-SEQ01` 用户注册登录对象级顺序图；关键对象包括 `LoginPage`、`AppRouter`、`AuthStorage`、`AuthController`、`AuthService`、`AuthRepository`、`JwtTokenService`、`AccountController`。 |
| 代码模块 | `apps/user_app/lib/features/auth/presentation/login_page.dart`；`apps/user_app/lib/app/router.dart`；`apps/user_app/lib/core/storage/auth_storage.dart`；`services/backend/src/main/java/com/aituan/auth`；`services/backend/src/main/java/com/aituan/common/security`。 |
| 主要接口 | `POST /api/open/auth/email-code`；`POST /api/open/auth/user/register`；`POST /api/open/auth/user/login/password`；`GET /api/open/auth/token/check`。 |
| 测试编号 | `UNIT-TC01`、`INT-TC01`、`E2E-TC01`。 |
| 测试资产 | 后端 `AuthApiIntegrationTest.java`、`SecurityBoundaryApiTest.java`；用户端 `core/utils/validator_test.dart`；E2E `tests/e2e/specs/uc01-auth-flow.spec.ts`。 |
| 测试结果 | 验证通过。最终测试报告记录 UC01-UC13 端到端测试全部通过，失败数为 0。 |

## 3. UC02 用户搜索筛选商家/商品并查看详情

| 追溯项 | 内容 |
| --- | --- |
| 需求编号 | `REQ-F-002` |
| 用户故事 | `US-02`：作为用户，我希望搜索和筛选商家或商品并查看详情，以便选择符合条件的服务。 |
| 用例编号 | `UC-02` / `UC02` |
| 主要参与者 | 用户、游客、地图与位置服务。 |
| 前置条件 | 平台存在已上架且可展示的商家或服务数据；商家、门店、商品服务和分类数据已初始化。 |
| 主成功流程 | 用户输入关键词或选择分类；系统获取位置或用户手选区域；用户设置距离、评分、销量、价格或营业状态等条件；系统执行搜索、筛选、排序和分页；用户选择商家或商品；系统展示商家资料、商品价格、库存、营业状态和适用规则。 |
| 备选/异常流程 | 定位失败时允许手选位置或降级为非距离排序；无匹配结果时返回空列表和重置建议；详情对象已下架或不可用时展示真实状态并限制交易入口。 |
| 可验证结果 | 用户获得符合条件且字段完整的结果详情，或得到可解释的空结果和降级内容；搜索结果、商家详情、商品或服务详情可打开，筛选和排序结果正确。 |
| 三层模型：系统级 | `SYS-SEQ-02` 商家/商品搜索筛选与详情查看系统级顺序图；对应需求说明书 3.6.2。 |
| 三层模型：概要/组件级 | 商家与商品子系统；发现/搜索接口组 `/api/app/discovery`；核心组件包括 `DiscoveryController`、`DiscoveryService`、`DiscoveryRepository`；支撑首页模块、推荐、搜索、商家详情、商品详情。 |
| 三层模型：详细/对象级 | `OBJ-SEQ02` 搜索筛选与详情查看对象级顺序图；关键对象包括 `SearchPage`、`SearchResultPage`、`ModulePage`、`TakeawayMerchantPage`、`ServiceMerchantPage`、`ItemDetailPage`、`DiscoveryController`、`DiscoveryService`、`DiscoveryRepository`。 |
| 代码模块 | `apps/user_app/lib/features/search`；`apps/user_app/lib/features/home`；`apps/user_app/lib/features/merchant`；`services/backend/src/main/java/com/aituan/discovery`。 |
| 主要接口 | `GET /api/app/discovery/home`；`GET /api/app/discovery/modules/{moduleCode}`；`GET /api/app/discovery/stores/search`；`GET /api/app/discovery/stores/{storeId}`；`GET /api/app/discovery/items/{itemId}`。 |
| 测试编号 | `UNIT-TC02`、`INT-TC02`、`E2E-TC02`。 |
| 测试资产 | 后端 `DiscoveryServiceTest.java`；用户端业务类型模型测试 `shared/enums/business_type_test.dart`；E2E `tests/e2e/specs/uc02-discovery-flow.spec.ts`。 |
| 测试结果 | 验证通过。最终测试报告记录首页模块、推荐、关键词搜索、业务类型筛选、商家详情等断言通过。 |

## 4. UC03 用户维护个人资料、地址和收藏

| 追溯项 | 内容 |
| --- | --- |
| 需求编号 | `REQ-F-003` |
| 用户故事 | `US-03`：作为用户，我希望维护个人资料、地址和收藏，以便保存个人信息和常用消费数据。 |
| 用例编号 | `UC-03` / `UC03` |
| 主要参与者 | 用户。 |
| 前置条件 | 用户已登录。 |
| 主成功流程 | 系统展示用户当前资料、地址和收藏；用户修改昵称、头像等允许编辑的资料；用户新增、编辑、删除地址或设置默认地址；用户收藏或取消收藏商家、商品或服务；系统校验并保存变更。 |
| 备选/异常流程 | 资料或地址字段不合法时拒绝保存并提示修正；删除默认地址后按规则选择新的默认地址或保持无默认地址；目标商家或商品已下架时保留历史收藏标识并限制消费操作；越权访问他人资料时拒绝请求。 |
| 可验证结果 | 合法修改被持久保存，默认地址唯一，收藏状态与用户操作一致，用户只能访问本人数据。 |
| 三层模型：系统级 | `SYS-ACT-03` 个人资料、地址和收藏维护系统级顺序图/活动图；对应需求说明书 3.6.3。 |
| 三层模型：概要/组件级 | 账号与用户资产子系统；账户接口组 `/api/app/account`；文件资源模块支撑头像上传；核心组件包括 `AccountController`、`AccountService`、`AccountRepository`、`FileStorageService`。 |
| 三层模型：详细/对象级 | `OBJ-SEQ03` 用户资料地址收藏对象级顺序图；关键对象包括 `ProfilePage`、`ProfileEditPage`、`AddressListPage`、`AddressEditPage`、`FavoritePage`、`AccountController`、`AccountService`、`AccountRepository`、`FileStorageService`。 |
| 代码模块 | `apps/user_app/lib/features/profile`；`apps/user_app/lib/features/address`；`apps/user_app/lib/features/favorite`；`services/backend/src/main/java/com/aituan/account`；`services/backend/src/main/java/com/aituan/common/file`。 |
| 主要接口 | `GET /api/app/account/profile`；`PUT /api/app/account/profile`；`GET /api/app/account/addresses`；`POST /api/app/account/addresses`；`PUT /api/app/account/addresses/{addressId}`；`DELETE /api/app/account/addresses/{addressId}`；`GET /api/app/account/favorites`；`POST /api/app/account/favorites`；`DELETE /api/app/account/favorites/{favoriteType}/{targetId}`。 |
| 测试编号 | `UNIT-TC03`、`INT-TC03`、`E2E-TC03`。 |
| 测试资产 | 后端 `CommonFileControllerTest.java`、`FileStorageServiceTest.java`；用户端 `web_unsupported_page_test.dart`、`app_bottom_action_bar_test.dart` 等基础页面回归；E2E `tests/e2e/specs/uc03-account-assets-flow.spec.ts`。 |
| 测试结果 | 验证通过。最终测试报告记录资料、地址、收藏增查改删闭环通过。 |

## 5. UC04 用户完成外卖点单、优惠结算和模拟支付，并查看订单详情

| 追溯项 | 内容 |
| --- | --- |
| 需求编号 | `REQ-F-004` |
| 用户故事 | `US-04`：作为用户，我希望完成外卖点单、优惠结算和模拟支付并查看订单详情，以便获得明确的交易结果。 |
| 用例编号 | `UC-04` / `UC04` |
| 主要参与者 | 用户、支付模拟服务、地图与位置服务。 |
| 前置条件 | 用户已登录；商家营业；购物项有效；用户具有可用收货地址；商品在售且地址可配送。 |
| 主成功流程 | 用户选择商品、规格和数量并加入购物车；进入结算页选择地址和可用优惠券；系统重新校验商品状态、价格、库存、优惠规则和配送可达性；系统计算原价、优惠、配送费和实付金额；用户提交订单并确认模拟支付；支付模拟服务返回成功结果；系统幂等更新支付和订单状态；用户查看包含明细、金额、地址和状态时间线的订单详情。 |
| 备选/异常流程 | 商品失效、价格变化、库存不足、地址不可达、起送价不足或优惠券无效时阻止提交并提示修正；重复提交不得生成重复订单；支付失败、超时或结果未知时保留可查询状态并允许重试。 |
| 可验证结果 | 成功时存在唯一订单、订单明细、金额快照和支付记录，订单详情与当前状态一致；失败时不发生重复订单、重复支付记录或错误库存扣减。 |
| 三层模型：系统级 | `SYS-SEQ-04` 外卖点单、优惠结算、模拟支付和详情查看系统级顺序图/活动图；对应需求说明书 3.6.4。 |
| 三层模型：概要/组件级 | 交易与履约子系统，联动商家与商品、会员优惠券、消息通知模块；核心组件包括 `TradeController`、`TradeService`、`TradeRepository`、`CouponService`、`StationMessagePublisher`。 |
| 三层模型：详细/对象级 | `OBJ-SEQ04` 外卖点单支付对象级顺序图；关键对象包括 `TakeawayMerchantPage`、`TakeawayCartSheet`、`CheckoutPage`、`TakeawayOrderDetailPage`、`TradeController`、`TradeService`、`TradeRepository`、`CouponService`、`StationMessagePublisher`。 |
| 代码模块 | `apps/user_app/lib/features/merchant/presentation/takeaway_merchant_page.dart`；`apps/user_app/lib/features/checkout/presentation/checkout_page.dart`；`apps/user_app/lib/features/order/presentation/takeaway_order_detail_page.dart`；`services/backend/src/main/java/com/aituan/trade`；`services/backend/src/main/java/com/aituan/coupon`；`services/backend/src/main/java/com/aituan/message`。 |
| 主要接口 | `GET /api/app/trade/cart`；`POST /api/app/trade/cart/items`；`POST /api/app/trade/checkout/preview`；`POST /api/app/trade/orders`；`POST /api/app/trade/orders/{orderId}/pay`；`GET /api/app/trade/orders/{orderId}`。 |
| 测试编号 | `UNIT-TC04`、`INT-TC04`、`E2E-TC04`。 |
| 测试资产 | 后端 `TradeApiIntegrationTest.java`、`CouponApiIntegrationTest.java`；用户端 `features/merchant/takeaway_amount_utils_test.dart`；E2E `tests/e2e/specs/uc04-takeaway-order-flow.spec.ts`。 |
| 测试结果 | 验证通过。最终测试报告记录外卖结算、下单、模拟支付和订单详情断言通过。 |

## 6. UC05 商家处理外卖订单并推进配送履约，用户接收订单状态消息

| 追溯项 | 内容 |
| --- | --- |
| 需求编号 | `REQ-F-005` |
| 用户故事 | `US-05`：作为用户，我希望商家处理订单并推进配送履约，同时接收状态消息，以便掌握外卖订单进度。 |
| 用例编号 | `UC-05` / `UC05` |
| 主要参与者 | 商家、用户、配送模拟服务、消息通知服务、平台管理员。 |
| 前置条件 | 已支付外卖订单进入商家待处理队列；商家拥有订单所属门店权限；用户具有可用消息接收渠道。 |
| 主成功流程 | 商家查看订单并接单；系统更新为制作中并向用户发送状态消息；商家完成备餐并标记待配送；系统创建模拟配送任务；配送模拟服务推进待取餐、配送中和已送达状态；系统在关键状态变化时向用户发送消息；用户查看订单进度；用户确认收货或系统超时自动完成。 |
| 备选/异常流程 | 商家拒单时订单进入取消或退款流程并通知用户；重复或越权处理被拒绝；配送数据异常时保留最近有效状态并允许平台管理员干预；消息发送失败时记录失败并允许重试，且不阻断订单状态流转。 |
| 可验证结果 | 订单主状态、配送子状态和用户可见时间线一致；关键节点产生可查询消息记录；订单最终形成完成、取消或退款闭环。 |
| 三层模型：系统级 | `SYS-SEQ-05` 商家处理、配送履约和状态通知系统级活动图；对应需求说明书 3.6.5。 |
| 三层模型：概要/组件级 | 交易与履约子系统、消息通知模块、配送模拟服务；核心组件包括 `TradeOpsController`、`TradeService`、`TradeRepository`、`TradeDeliveryScheduler`、`StationMessagePublisher`、`MessageService`。 |
| 三层模型：详细/对象级 | `OBJ-STATE05` 外卖订单与配送履约对象状态图；关键对象包括 `OrderPage`、`FulfillmentPage`、`DeliveryTrackingPage`、`TradeOpsController`、`TradeService`、`TradeRepository`、`TradeDeliveryScheduler`、`StationMessagePublisher`、`MessageService`。 |
| 代码模块 | `apps/merchant_web/src/pages/OrderPage.vue`；`apps/merchant_web/src/pages/FulfillmentPage.vue`；`apps/user_app/lib/features/order/presentation/delivery_tracking_page.dart`；`apps/user_app/lib/features/message/presentation/message_page.dart`；`services/backend/src/main/java/com/aituan/trade`；`services/backend/src/main/java/com/aituan/message`。 |
| 主要接口 | `GET /api/merchant/trade/orders`；`POST /api/merchant/trade/orders/{orderId}/accept`；`POST /api/merchant/trade/orders/{orderId}/prepare`；`POST /api/merchant/trade/orders/{orderId}/ready`；`POST /api/merchant/trade/orders/{orderId}/delivery/advance`；`GET /api/app/trade/orders/{orderId}/delivery/timeline`；`GET /api/app/message/station`。 |
| 测试编号 | `UNIT-TC05`、`INT-TC05`、`E2E-TC05`。 |
| 测试资产 | 后端 `TradeApiIntegrationTest.java`、`MessageApiIntegrationTest.java`、`StationMessagePublisherTest.java`；用户端 `features/order/takeaway_fulfillment_text_test.dart`；E2E `tests/e2e/specs/uc05-delivery-fulfillment-flow.spec.ts`。 |
| 测试结果 | 验证通过。最终测试报告记录商家接单、备餐、出餐、配送推进、完成订单、配送时间线和站内消息断言通过。 |

## 7. UC06 用户购买非外卖服务并获得券码凭证

| 追溯项 | 内容 |
| --- | --- |
| 需求编号 | `REQ-F-006` |
| 用户故事 | `US-06`：作为用户，我希望购买非外卖服务并获得券码凭证，以便后续到店使用。 |
| 用例编号 | `UC-06` / `UC06` |
| 主要参与者 | 用户、支付模拟服务。 |
| 前置条件 | 服务已上架且未过期；库存、限购和购买资格满足规则。 |
| 主成功流程 | 用户确认服务项目、数量和适用优惠；系统校验价格、库存、限购和使用规则；系统创建非外卖订单；用户完成模拟支付；系统生成与订单关联的唯一券码凭证；用户在订单详情中查看券码、有效期和使用规则。 |
| 备选/异常流程 | 库存不足、服务过期、超过限购或支付失败时不生成有效券码；券码生成失败时订单保持可补偿状态并记录异常；重复支付回调不得生成重复券码。 |
| 可验证结果 | 成功时用户获得唯一且可查询的券码凭证，订单、支付和券码状态一致。 |
| 三层模型：系统级 | `SYS-SEQ-06` 非外卖服务购买与券码生成系统级顺序图；对应需求说明书 3.6.6。 |
| 三层模型：概要/组件级 | 交易与履约子系统中的非外卖服务与券码能力；核心组件包括 `TradeController`、`TradeService`、`TradeRepository`，并通过统一订单模型、`order_voucher` 记录区分券码履约方式。 |
| 三层模型：详细/对象级 | `OBJ-SEQ06` 非外卖购买出券对象级顺序图；关键对象包括 `ServiceMerchantPage`、`ItemDetailPage`、`CheckoutPage`、`ServiceOrderDetailPage`、`VoucherDetailPage`、`TradeController`、`TradeService`、`TradeRepository`。 |
| 代码模块 | `apps/user_app/lib/features/merchant/presentation/service_merchant_page.dart`；`apps/user_app/lib/features/merchant/presentation/item_detail_page.dart`；`apps/user_app/lib/features/order/presentation/service_order_detail_page.dart`；`apps/user_app/lib/features/order/presentation/voucher_detail_page.dart`；`services/backend/src/main/java/com/aituan/trade`。 |
| 主要接口 | `POST /api/app/trade/orders`；`POST /api/app/trade/orders/{orderId}/pay`；`GET /api/app/trade/orders/{orderId}`；`GET /api/merchant/trade/vouchers/{voucherCode}`。 |
| 测试编号 | `UNIT-TC06`、`INT-TC06`、`E2E-TC06`。 |
| 测试资产 | 后端 `TradeApiIntegrationTest.java`；用户端业务类型与订单模型相关测试；E2E `tests/e2e/specs/uc06-service-voucher-flow.spec.ts`。 |
| 测试结果 | 验证通过。最终测试报告记录非外卖服务订单支付后出券、商家查询与核销、订单状态回查断言通过。 |

## 8. UC07 用户预约到店/团购服务并由商家核销完成服务

| 追溯项 | 内容 |
| --- | --- |
| 需求编号 | `REQ-F-007` |
| 用户故事 | `US-07`：作为用户，我希望预约到店或团购服务并由商家核销，以便完成预约服务履约。 |
| 用例编号 | `UC-07` / `UC07` |
| 主要参与者 | 用户、商家、支付模拟服务。 |
| 前置条件 | 服务可预约；用户具有有效订单或券码；商家已配置履约和核销规则；预约日期或时段可用。 |
| 主成功流程 | 用户选择门店、资源和时间并提交预约；系统校验券码、预约资格、资源可用性和时间冲突；商家确认预约；系统占用资源并向用户展示预约结果；用户到店出示券码或预约凭证；商家核销；系统将预约和订单更新为已使用、已完成。 |
| 备选/异常流程 | 资源冲突或预约已满时要求重新选择；商家拒绝或超时未确认时释放资源；券码无效、过期、已核销或已退款时拒绝核销；规则允许时用户可取消或改期并进入 UC08。 |
| 可验证结果 | 预约、资源占用、券码和订单状态一致，同一凭证不得重复核销；商家确认或核销后订单完成。 |
| 三层模型：系统级 | `SYS-ACT-07` 到店/团购服务预约与核销系统级活动图；对应需求说明书 3.6.7。 |
| 三层模型：概要/组件级 | 交易与履约子系统中的预约与券码核销能力；核心组件包括 `TradeController`、`TradeOpsController`、`TradeService`、`TradeRepository`。 |
| 三层模型：详细/对象级 | `OBJ-SEQ07` 预约与核销对象级状态图；关键对象包括 `BookingDetailPage`、`VoucherPage`、`BookingPage`、`TradeController`、`TradeOpsController`、`TradeService`、`TradeRepository`。 |
| 代码模块 | `apps/user_app/lib/features/order/presentation/booking_detail_page.dart`；`apps/merchant_web/src/pages/VoucherPage.vue`；`apps/merchant_web/src/pages/BookingPage.vue`；`services/backend/src/main/java/com/aituan/trade`。 |
| 主要接口 | `GET /api/app/trade/orders/{orderId}/booking`；`POST /api/app/trade/orders/{orderId}/booking`；`GET /api/merchant/trade/bookings`；`POST /api/merchant/trade/orders/{orderId}/booking/confirm`；`POST /api/merchant/trade/vouchers/{voucherCode}/redeem`。 |
| 测试编号 | `UNIT-TC07`、`INT-TC07`、`E2E-TC07`。 |
| 测试资产 | 后端 `TradeApiIntegrationTest.java`；E2E `tests/e2e/specs/uc07-booking-redeem-flow.spec.ts`。 |
| 测试结果 | 验证通过。最终测试报告记录预约记录生成、商家确认、券码/预约状态和订单完成状态断言通过。 |

## 9. UC08 用户申请取消与退款，商家或平台处理退款

| 追溯项 | 内容 |
| --- | --- |
| 需求编号 | `REQ-F-008` |
| 用户故事 | `US-08`：作为用户，我希望申请取消与退款并获得商家或平台处理结果，以便完成逆向交易。 |
| 用例编号 | `UC-08` / `UC08` |
| 主要参与者 | 用户、商家、支付模拟服务、平台管理员。 |
| 前置条件 | 订单存在且属于当前用户；订单状态符合取消或退款规则。 |
| 主成功流程 | 用户选择订单和原因并提交；系统读取订单类型、当前状态和退改规则；未支付且符合规则的订单直接取消；需要审核的申请发送给商家或平台；处理方同意退款后系统创建退款记录；支付模拟服务返回退款结果；系统关闭关联库存占用、预约资源、券码或配送任务并更新订单状态；系统通知用户处理结果。 |
| 备选/异常流程 | 超过退改期限、券码已核销或订单已完成时拒绝申请；商家拒绝时用户可按规则提交平台复核；处理超时可升级给平台；退款结果未知时保持退款中并允许重试或人工修正。 |
| 可验证结果 | 订单最终进入已取消、已退款或明确拒绝状态，且关联资源和子状态保持一致；优惠券、券码、预约等关联资源同步处理。 |
| 三层模型：系统级 | `SYS-ACT-08` 取消与退款申请处理系统级活动图；对应需求说明书 3.6.8。 |
| 三层模型：概要/组件级 | 交易与履约子系统联动会员优惠券、券码、预约、配送任务和平台治理支撑；核心组件包括 `TradeController`、`TradeOpsController`、`TradeService`、`TradeRepository`、`CouponService`。 |
| 三层模型：详细/对象级 | `OBJ-STATE08` 取消与退款状态图；关键对象包括 `TakeawayOrderDetailPage`、`ServiceOrderDetailPage`、`OrderPage`、`BookingsPage`、`TradeController`、`TradeOpsController`、`TradeService`、`TradeRepository`、`CouponService`。 |
| 代码模块 | `apps/user_app/lib/features/order`；`apps/merchant_web/src/pages/OrderPage.vue`；`apps/merchant_web/src/pages/BookingPage.vue`；`apps/admin_web/src/pages/OrdersPage.vue`；`apps/admin_web/src/pages/BookingsPage.vue`；`services/backend/src/main/java/com/aituan/trade`；`services/backend/src/main/java/com/aituan/coupon`。 |
| 主要接口 | `POST /api/app/trade/orders/{orderId}/cancel`；`POST /api/app/trade/orders/{orderId}/refund`；`POST /api/merchant/trade/orders/{orderId}/refund`；`POST /api/admin/trade/orders/{orderId}/refund`。 |
| 测试编号 | `UNIT-TC08`、`INT-TC08`、`E2E-TC08`。 |
| 测试资产 | 后端 `TradeApiIntegrationTest.java`、`TradeGrowthServiceTest.java`、`CouponApiIntegrationTest.java`；E2E `tests/e2e/specs/uc08-refund-flow.spec.ts`。 |
| 测试结果 | 验证通过。最终测试报告记录用户退款、商家人工退款、券码/预约/优惠券/库存释放断言通过。 |

## 10. UC09 用户完成订单后发布评价，商家回复，平台审核或治理评价

| 追溯项 | 内容 |
| --- | --- |
| 需求编号 | `REQ-F-009` |
| 用户故事 | `US-09`：作为已完成订单的用户，我希望发布评价并获得商家回复，同时由平台治理违规评价。 |
| 用例编号 | `UC-09` / `UC09` |
| 主要参与者 | 用户、商家、平台管理员。 |
| 前置条件 | 订单属于当前用户并已完成；评价资格未被使用或仍允许追评。 |
| 主成功流程 | 用户提交评分、文字和图片；系统校验订单关联和内容规则；合格评价发布；商家查看并公开回复；系统保存回复记录；平台对举报、规则命中或抽检评价进行审核，并执行保留、屏蔽、删除或复核。 |
| 备选/异常流程 | 订单未完成、重复评价、内容违规或恶意刷评时拒绝发布或转人工审核；商家回复违规时执行相同治理规则；平台处置失败时保持原状态并记录异常。 |
| 可验证结果 | 评价与真实订单强关联，回复、举报和审核记录可追踪；已完成订单可评价，商家可回复，平台可审核或治理。 |
| 三层模型：系统级 | `SYS-SEQ-09` 评价发布、商家回复与平台治理系统级顺序图；对应需求说明书 3.6.9。 |
| 三层模型：概要/组件级 | 互动、客服与投诉子系统；评价治理接口分布在用户端、商家端和后台端；核心组件包括 `InteractionController`、`InteractionMerchantController`、`InteractionAdminController`、`InteractionService`。 |
| 三层模型：详细/对象级 | `OBJ-SEQ09` 评价发布回复审核对象级顺序图；关键对象包括 `ReviewPublishPage`、`MyReviewsPage`、`ReviewDetailPage`、`ReviewPage`、`ReviewsPage`、`InteractionController`、`InteractionMerchantController`、`InteractionAdminController`、`InteractionService`、`InteractionRepository`。 |
| 代码模块 | `apps/user_app/lib/features/review`；`apps/merchant_web/src/pages/ReviewPage.vue`；`apps/admin_web/src/pages/ReviewsPage.vue`；`services/backend/src/main/java/com/aituan/interaction`。 |
| 主要接口 | `POST /api/app/interaction/orders/{orderId}/review`；`GET /api/app/interaction/reviews/me`；`GET /api/app/interaction/reviews/{id}`；`POST /api/merchant/ops/reviews/{id}/reply`；`POST /api/admin/governance/reviews/{id}/audit`。 |
| 测试编号 | `UNIT-TC09`、`INT-TC09`、`E2E-TC09`。 |
| 测试资产 | 后端 `InteractionServiceTest.java`；E2E `tests/e2e/specs/uc09-review-governance-flow.spec.ts`。 |
| 测试结果 | 验证通过。最终测试报告记录用户评价发布、商家回复、后台屏蔽、公开列表不可见、后台恢复等断言通过。 |

## 11. UC10 用户提交投诉，商家或平台受理、处理并关闭

| 追溯项 | 内容 |
| --- | --- |
| 需求编号 | `REQ-F-010` |
| 用户故事 | `US-10`：作为用户，我希望提交投诉并获得商家或平台的受理、处理和关闭结果。 |
| 用例编号 | `UC-10` / `UC10` |
| 主要参与者 | 用户、商家、平台管理员、消息通知服务。 |
| 前置条件 | 用户已登录；投诉对象存在且与用户具有可验证关联；投诉内容符合提交规则。 |
| 主成功流程 | 用户选择投诉对象、问题类型并提交说明和证据；系统创建唯一投诉工单并通知责任方；商家或平台受理工单；处理方与用户沟通并记录处理动作；需要时工单转派或升级；处理方给出解决方案；用户确认或系统按规则关闭工单；系统通知各方并保留完整记录。 |
| 备选/异常流程 | 材料不足时要求补充；重复投诉时合并或提示已有工单；商家超时未处理时自动升级平台；用户不接受结果时可申请复核；关闭失败时保持处理中并记录异常；已关闭投诉不能重复处理。 |
| 可验证结果 | 投诉具有唯一工单号和受理、处理、关闭结果，处理人、时间、证据、沟通和状态变化均可追踪。 |
| 三层模型：系统级 | `SYS-ACT-10` 投诉受理、处理与关闭系统级活动图；对应需求说明书 3.6.10。 |
| 三层模型：概要/组件级 | 互动、客服与投诉子系统，平台治理支撑参与处理；核心组件包括 `ComplaintController`、`ComplaintMerchantController`、`ComplaintAdminController`、`ComplaintService`、`ComplaintRepository`、`FileStorageService`。 |
| 三层模型：详细/对象级 | `OBJ-STATE10` 投诉工单顺序图/状态图；关键对象包括 `ComplaintSubmitPage`、`ComplaintListPage`、`ComplaintDetailPage`、`ComplaintsPage`、`ComplaintController`、`ComplaintMerchantController`、`ComplaintAdminController`、`ComplaintService`、`ComplaintRepository`、`FileStorageService`。 |
| 代码模块 | `apps/user_app/lib/features/complaint`；`apps/merchant_web/src/pages/ComplaintsPage.vue`；`apps/admin_web/src/pages/ComplaintsPage.vue`；`services/backend/src/main/java/com/aituan/complaint`；`services/backend/src/main/java/com/aituan/common/file`。 |
| 主要接口 | `POST /api/app/complaints`；`GET /api/app/complaints`；`GET /api/app/complaints/{id}`；`POST /api/app/complaints/{id}/supplements`；`GET /api/merchant/ops/complaints`；`POST /api/admin/governance/complaints/{id}/accept`；`POST /api/admin/governance/complaints/{id}/resolve`；`POST /api/admin/governance/complaints/{id}/close`。 |
| 测试编号 | `UNIT-TC10`、`INT-TC10`、`E2E-TC10`。 |
| 测试资产 | 后端 `ComplaintApiIntegrationTest.java`、`ComplaintServiceTest.java`、`CommonFileControllerTest.java`；E2E `tests/e2e/specs/uc10-complaint-flow.spec.ts`。 |
| 测试结果 | 验证通过。最终测试报告记录投诉提交、平台受理、处理、关闭和处理记录追溯断言通过。 |

## 12. UC11 用户联系客服，AI、商家或平台客服进行回复

| 追溯项 | 内容 |
| --- | --- |
| 需求编号 | `REQ-F-011` |
| 用户故事 | `US-11`：作为用户，我希望联系客服并获得 AI、商家或平台客服回复，以便解决咨询或订单问题。 |
| 用例编号 | `UC-11` / `UC11` |
| 主要参与者 | 用户、AI 客服、商家客服、平台客服、外部 AI 模型服务。 |
| 前置条件 | 用户可访问客服入口；关联商家或订单存在；客服会话可创建；AI 配置可用或具备降级规则。 |
| 主成功流程 | 用户创建或进入客服会话并提交问题；系统识别会话归属和关联订单；AI 客服生成并返回基础答复；需要人工处理时系统转接商家客服或平台客服；客服查看上下文并回复；用户接收回复；系统保存会话、消息和处理状态。 |
| 备选/异常流程 | AI 不可用、超时或低置信度时直接转人工或返回模板答复；商家客服无法处理时升级平台客服；发送失败时允许重试；违规内容被拦截；未解决会话保持待处理状态；已关闭会话不能继续发送普通消息。 |
| 可验证结果 | 用户获得 AI、商家或平台客服的可识别回复，会话参与方、消息、关联订单、转接和处理状态均可查询。 |
| 三层模型：系统级 | `SYS-SEQ-11` AI、商家客服与平台客服回复系统级顺序图；对应需求说明书 3.6.11。 |
| 三层模型：概要/组件级 | 互动、客服与投诉子系统、AI 辅助模块、消息通知模块；核心组件包括 `SupportController`、`SupportMerchantController`、`SupportAdminController`、`SupportService`、`AiAssistantController`、`AiAgentService`、`AiSkillRegistry`。 |
| 三层模型：详细/对象级 | `OBJ-SEQ11` 客服与 AI 回复对象级顺序图；关键对象包括 `SupportSessionsPage`、`SupportChatPage`、`AssistantPage`、`SupportController`、`SupportMerchantController`、`SupportAdminController`、`SupportService`、`AiAssistantController`、`AiAgentService`、`AiSkillRegistry`。 |
| 代码模块 | `apps/user_app/lib/features/support`；`apps/user_app/lib/features/assistant`；`apps/merchant_web/src/pages/SessionsPage.vue`；`apps/admin_web/src/pages/PlatformSupportPage.vue`；`services/backend/src/main/java/com/aituan/support`；`services/backend/src/main/java/com/aituan/ai`。 |
| 主要接口 | `GET /api/app/support/sessions`；`POST /api/app/support/sessions`；`GET /api/app/support/sessions/{id}`；`POST /api/app/support/sessions/{id}/messages`；`POST /api/app/ai/assistant/message`；`GET /api/merchant/ops/sessions`；`POST /api/merchant/ops/sessions/{id}/messages`。 |
| 测试编号 | `UNIT-TC11`、`INT-TC11`、`E2E-TC11`。 |
| 测试资产 | 后端 `SupportServiceTest.java`、`AiAgentServiceTest.java`；用户端 `features/support/support_repository_models_test.dart`；E2E `tests/e2e/specs/uc11-support-ai-flow.spec.ts`。 |
| 测试结果 | 验证通过。最终测试报告记录 AI 降级回复、转人工、商家回复、平台介入和三方消息断言通过。 |

## 13. UC12 用户查看会员成长值、领取并在下单时使用优惠券

| 追溯项 | 内容 |
| --- | --- |
| 需求编号 | `REQ-F-012` |
| 用户故事 | `US-12`：作为用户，我希望查看会员成长值、领取优惠券并在下单时使用，以便获得会员和优惠权益。 |
| 用例编号 | `UC-12` / `UC12` |
| 主要参与者 | 用户。 |
| 前置条件 | 用户已登录；平台存在有效会员规则或可领取优惠券；优惠券模板存在且符合领取或使用规则。 |
| 主成功流程 | 用户查看当前会员等级、成长值、变动记录和权益；用户选择可领取优惠券；系统校验领取资格、数量和有效期并发放券实例；用户在结算时查看可用优惠券并选择使用；系统校验适用范围、门槛、有效期和互斥规则；系统计算优惠金额；订单支付成功后将优惠券标记为已使用并按规则增加成长值。 |
| 备选/异常流程 | 领取资格不满足、库存已空或重复领取时拒绝发放；优惠券过期、不满足门槛或与其他优惠互斥时不可使用；订单取消或支付失败时按规则释放优惠券；成长值更新失败时记录待补偿任务。 |
| 可验证结果 | 会员等级和成长值可查询，优惠券领取与使用状态唯一且与订单一致，优惠金额计算可复核。 |
| 三层模型：系统级 | `SYS-ACT-12` 会员成长值查看、优惠券领取与使用系统级顺序图/活动图；对应需求说明书 3.6.12。 |
| 三层模型：概要/组件级 | 账号与用户资产子系统、会员与优惠券模块、交易与履约子系统；核心组件包括 `MemberController`、`CouponController`、`MemberService`、`CouponService`、`CouponRepository`、`TradeService`。 |
| 三层模型：详细/对象级 | `OBJ-SEQ12` 会员优惠券对象级顺序图；关键对象包括 `MemberCenterPage`、`CouponPage`、`CouponClaimPage`、`CouponSelectorPage`、`CheckoutPage`、`MemberController`、`CouponController`、`MemberService`、`CouponService`、`CouponRepository`。 |
| 代码模块 | `apps/user_app/lib/features/member`；`apps/user_app/lib/features/coupon`；`apps/user_app/lib/features/checkout`；`services/backend/src/main/java/com/aituan/member`；`services/backend/src/main/java/com/aituan/coupon`；`services/backend/src/main/java/com/aituan/trade`。 |
| 主要接口 | `GET /api/app/account/member/info`；`GET /api/app/account/coupons`；`GET /api/app/account/coupons/available`；`POST /api/app/account/coupons/{templateId}/claim`；`GET /api/app/account/coupons/usable-for-order`。 |
| 测试编号 | `UNIT-TC12`、`INT-TC12`、`E2E-TC12`。 |
| 测试资产 | 后端 `CouponApiIntegrationTest.java`、`TradeGrowthServiceTest.java`；用户端 `member_coupon_models_test.dart`、`features/coupon/coupon_repository_test.dart`；E2E `tests/e2e/specs/uc12-member-coupon-flow.spec.ts`。 |
| 测试结果 | 验证通过。最终测试报告记录会员信息、领券、订单可用券、下单抵扣、券状态变更和成长值增长断言通过。 |

## 14. UC13 商家维护门店资料、履约规则和商品/服务目录，并管理上下架状态

| 追溯项 | 内容 |
| --- | --- |
| 需求编号 | `REQ-F-013` |
| 用户故事 | `US-13`：作为商家，我希望维护门店资料、履约规则和商品或服务目录，并管理上下架状态，以便持续经营。 |
| 用例编号 | `UC-13` / `UC13` |
| 主要参与者 | 商家、图片与对象存储服务。 |
| 前置条件 | 商家账号有效并拥有目标门店权限；商家端门店资料、履约设置、商品管理页面可访问。 |
| 主成功流程 | 商家维护门店名称、地址、营业时间、联系方式和营业状态；商家配置配送范围、起送规则、券码有效期、预约时段、核销和退改规则；商家新增或修改商品、套餐和服务的分类、价格、库存、图片和说明；系统校验完整性与规则一致性；商家保存并上架或下架；系统记录版本和操作日志；用户搜索和详情页读取更新后的门店和商品服务信息。 |
| 备选/异常流程 | 字段缺失、图片不合法、价格库存异常、履约规则冲突或账号越权时拒绝保存或发布；门店停业或商品下架不得破坏既有订单快照和已售凭证；并发修改时要求刷新或按版本控制处理。 |
| 可验证结果 | 合法门店资料、履约规则和目录内容成功保存，用户侧仅展示可用内容；不合法或越权操作被拒绝且不影响既有订单。 |
| 三层模型：系统级 | `SYS-ACT-13` 门店资料、履约规则、目录与上下架维护系统级顺序图/活动图；对应需求说明书 3.6.13。 |
| 三层模型：概要/组件级 | 商家与商品子系统、交易与履约子系统、文件资源模块；核心组件包括 `MerchantController`、`CatalogController`、`TradeOpsController`、`MerchantService`、`CatalogService`、`TradeService`、`FileStorageService`。 |
| 三层模型：详细/对象级 | `OBJ-SEQ13` 商家门店商品维护对象级顺序图；关键对象包括 `StorePage`、`FulfillmentPage`、`CatalogPage`、`MerchantController`、`CatalogController`、`TradeOpsController`、`MerchantService`、`CatalogService`、`TradeService`、`FileStorageService`。 |
| 代码模块 | `apps/merchant_web/src/pages/StorePage.vue`；`apps/merchant_web/src/pages/FulfillmentPage.vue`；`apps/merchant_web/src/pages/CatalogPage.vue`；`services/backend/src/main/java/com/aituan/merchant`；`services/backend/src/main/java/com/aituan/catalog`；`services/backend/src/main/java/com/aituan/trade`；`services/backend/src/main/java/com/aituan/common/file`。 |
| 主要接口 | `GET /api/merchant/profile/me`；`PUT /api/merchant/profile/me`；`GET /api/merchant/stores/current`；`PUT /api/merchant/stores/current`；`POST /api/merchant/stores/current/cover`；`GET /api/merchant/catalog/items`；`POST /api/merchant/catalog/items`；`PUT /api/merchant/catalog/items/{itemId}`；`POST /api/merchant/catalog/items/{itemId}/status`；`GET /api/merchant/trade/stores/{storeId}/delivery-rule`；`POST /api/merchant/trade/stores/{storeId}/delivery-rule`。 |
| 测试编号 | `UNIT-TC13`、`INT-TC13`、`E2E-TC13`。 |
| 测试资产 | 商家端 `apps/merchant_web/src/api.test.ts`；后端文件上传与商家商品相关服务/API 测试；E2E `tests/e2e/specs/uc13-merchant-catalog-flow.spec.ts`。 |
| 测试结果 | 验证通过。最终测试报告记录门店资料、履约规则、分类、商品新增编辑、下架与上架状态回查断言通过。 |

## 15. 总体测试结果依据

| 测试层级 | 测试资产 | 最终结果 |
| --- | --- | --- |
| 单元测试 | `services/backend/src/test/java`、`apps/user_app/test`、`apps/merchant_web/src/api.test.ts`、`apps/admin_web/src/api.test.ts` | 最终测试报告记录全部通过。 |
| 集成/API 测试 | 后端 JUnit、Spring Boot Test、MockMvc 测试类 | 最终测试报告记录全部通过。 |
| 端到端测试 | `tests/e2e/specs` 下 UC01-UC13 共 13 个 spec 文件、15 条 Playwright 测试 | 最终测试报告记录 15 条全部通过，失败数为 0。 |
| CI 门禁 | `.github/workflows/ci.yml` 中后端、前端、Flutter、E2E 自动化测试入口 | 测试失败时流水线停止；最终报告结论为满足本阶段验收要求。 |

标准执行入口如下：

```powershell
# 后端单元 / API / 集成测试
mvn -f services/backend/pom.xml -Dmaven.repo.local=D:/aituan_cache/m2 test

# Flutter 用户端测试
$env:PUB_CACHE="D:/aituan_cache/pub"
flutter test apps/user_app

# 商家端和后台端 Web 测试
npm --prefix apps/merchant_web --cache D:/aituan_cache/npm test
npm --prefix apps/admin_web --cache D:/aituan_cache/npm test

# UC01-UC13 端到端测试
npm test --prefix tests/e2e
```
